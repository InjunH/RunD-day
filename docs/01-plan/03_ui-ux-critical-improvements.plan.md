# Plan: UI/UX 5대 긴급 개선사항

## 📋 Document Information

- **Feature Name**: UI/UX Critical Improvements (5-Point Enhancement)
- **Document Type**: Plan (PDCA - Plan Phase)
- **Created**: 2026-01-21
- **Status**: Planning
- **Priority**: 🔴 HIGH (사용자 경험 저해 요소)

---

## 1. Executive Summary

RunD-day 마라톤 일정 서비스의 사용자 경험을 저해하는 5가지 긴급 이슈를 해결합니다.
현재 발견된 문제들은 데이터 표시, 필터링, URL 처리 등 핵심 기능에서 발생하고 있으며,
사용자 혼란을 야기하고 서비스 신뢰도를 저하시키고 있습니다.

**핵심 가치**: 정확한 정보 제공 + 직관적인 사용성 향상

---

## 2. Problem Statement (문제 정의)

### 2.1 발견된 5대 문제점

| # | 문제 | 현상 | 영향도 | 우선순위 |
|---|------|------|--------|---------|
| 1 | 과거 대회 상단 표시 | 종료된 대회가 제일 위에 노출됨 | 🔴 HIGH | P0 |
| 2 | 국내/국외 구분 불가 | 국외 대회 필터링 방법 없음 | 🔴 HIGH | P0 |
| 3 | 잘못된 URL 생성 | `localhost:3300/www.example.com` 형식 오류 | 🔴 CRITICAL | P0 |
| 4 | 비일관적 거리 표현 | "풀", "하프" 혼용 (영문 UI와 불일치) | 🟡 MEDIUM | P1 |
| 5 | 국외 대회 지역 오표기 | 모든 해외 대회가 "기타"로 표시됨 | 🟡 MEDIUM | P1 |

### 2.2 사용자 시나리오별 영향

**시나리오 1: 서울 마라톤 찾기**
- ❌ 문제: 2026-01-16 종료된 Doha Marathon이 최상단 노출
- ✅ 기대: 다가오는 서울 마라톤들이 날짜순 정렬

**시나리오 2: 국내 대회만 보기**
- ❌ 문제: 필터 없음, 스크롤로 수동 구분 필요
- ✅ 기대: "국내/국외" 토글로 즉시 필터링

**시나리오 3: 대회 등록 페이지 이동**
- ❌ 문제: URL 클릭 시 `http://localhost:3300/www.dohamarathonooredoo.com` 에러
- ✅ 기대: 정상적으로 외부 사이트 새탭 열림

**시나리오 4: 풀/하프 마라톤 구분**
- ❌ 문제: "풀" (한글) vs "Full" (영문) 혼용으로 혼란
- ✅ 기대: 일관된 영문 표기 (Full, Half)

**시나리오 5: 해외 대회 위치 확인**
- ❌ 문제: "기타 · Qatar" → 위치 아이콘 옆이 의미 없음
- ✅ 기대: "해외 · Qatar" → 명확한 구분

---

## 3. Objectives (목표)

### 3.1 비즈니스 목표

1. **사용성 개선**: 사용자가 원하는 대회를 3초 내 찾을 수 있도록
2. **신뢰도 향상**: 정확한 정보와 올바른 링크 제공
3. **이탈률 감소**: URL 오류로 인한 사용자 이탈 제거
4. **국제화 대응**: 국내외 대회 통합 관리 기반 구축

### 3.2 기술 목표

1. **날짜 기반 정렬**: 과거 대회 자동 필터링 로직 구현
2. **필터 확장**: 국내/국외 토글 필터 추가
3. **URL 정규화**: 등록 URL 자동 보정 로직 구현
4. **데이터 일관성**: 거리 표기 통일 (Full/Half)
5. **지역 분류 개선**: 국외 대회 → "해외" 카테고리 자동 매핑

### 3.3 성공 지표 (KPI)

| 지표 | 현재 | 목표 | 측정 방법 |
|------|------|------|---------|
| 과거 대회 노출 | 100% (상단 표시) | 0% (필터링) | 첫 화면 대회 날짜 확인 |
| URL 클릭 실패율 | ~30% (추정) | 0% | 등록 버튼 클릭 추적 |
| 국외 대회 구분 가능성 | 불가능 | 100% | 필터 사용 가능 여부 |
| 거리 표기 일관성 | 혼용 | 100% 통일 | 데이터 검증 |
| 사용자 문의 감소 | - | -50% | 고객 지원 티켓 |

---

## 4. Scope (범위)

### 4.1 In-Scope (포함 사항)

#### Feature 1: 과거 대회 필터링
- [ ] App.tsx: `filteredMarathons` 로직에 날짜 필터 추가
- [ ] 오늘 날짜 기준 과거 대회 제외
- [ ] 즐겨찾기는 과거 대회도 유지 (사용자가 저장한 이력)

#### Feature 2: 국내/국외 필터
- [ ] constants.ts: `COUNTRIES` 상수 추가 (`['국내', '해외']`)
- [ ] FilterBar.tsx: 국가 필터 드롭다운 추가
- [ ] App.tsx: 필터 상태에 `countries` 추가
- [ ] 필터 로직: `country` 필드 기반 필터링

#### Feature 3: URL 정규화
- [ ] hooks/useMarathons.ts: `transformEvent` 함수 수정
- [ ] URL 정규화 로직:
  ```typescript
  const normalizedUrl = url.startsWith('http')
    ? url
    : `https://${url}`;
  ```
- [ ] 빈 URL 처리: `registrationUrl || ''`

#### Feature 4: 거리 표현 통일
- [ ] scripts/scrapers/processors/normalizer.ts: 거리 매핑 수정
  - `"풀코스"` → `"Full"`
  - `"하프"` → `"Half"`
- [ ] constants.ts: `DISTANCES` 배열 업데이트
- [ ] App.tsx: 필터 로직에서 `Full`, `Half` 처리

#### Feature 5: 국외 지역 표시
- [ ] hooks/useMarathons.ts: `transformEvent` 로직 수정
  ```typescript
  region: apiEvent.country === 'INTL' ? '해외' : (apiEvent.region || '기타')
  ```
- [ ] constants.ts: `REGIONS` 배열에 `'해외'` 추가
- [ ] MarathonCard.tsx: 지역 표시 확인 (변경 불필요)

### 4.2 Out-of-Scope (제외 사항)

- ❌ 과거 대회 아카이브 페이지 (Phase 2)
- ❌ 다국어 지원 (Phase 2)
- ❌ 거리 필터 드롭다운 UI 구현 (별도 작업)
- ❌ 대회 상세 모달 (별도 작업)
- ❌ URL 유효성 검증 (서버 사이드 작업)

---

## 5. Technical Approach (기술 접근 방식)

### 5.1 아키텍처 개요

```
┌─────────────────────────────────────────┐
│  Data Layer (JSON + Hook)               │
│  ┌─────────────────────────────────┐   │
│  │ useMarathons.ts                 │   │
│  │ - transformEvent() 수정         │   │
│  │   · URL 정규화                  │   │
│  │   · 지역 "해외" 매핑            │   │
│  └─────────────────────────────────┘   │
└─────────────────────────────────────────┘
          ↓
┌─────────────────────────────────────────┐
│  Filter Layer (App.tsx)                 │
│  ┌─────────────────────────────────┐   │
│  │ filteredMarathons useMemo       │   │
│  │ - 날짜 필터 추가 (과거 제외)   │   │
│  │ - 국가 필터 추가                │   │
│  │ - 거리 필터 Full/Half 대응     │   │
│  └─────────────────────────────────┘   │
└─────────────────────────────────────────┘
          ↓
┌─────────────────────────────────────────┐
│  UI Layer (FilterBar + Card)            │
│  ┌─────────────────────────────────┐   │
│  │ FilterBar.tsx                   │   │
│  │ - 국가 드롭다운 추가            │   │
│  │                                  │   │
│  │ MarathonCard.tsx                │   │
│  │ - URL 클릭 처리 (변경 없음)    │   │
│  └─────────────────────────────────┘   │
└─────────────────────────────────────────┘
```

### 5.2 핵심 로직

#### 5.2.1 날짜 필터링 (Feature 1)

```typescript
// App.tsx - filteredMarathons
const filteredMarathons = useMemo(() => {
  const today = new Date();
  today.setHours(0, 0, 0, 0); // 오늘 0시 기준

  return marathonData.filter(m => {
    // 1. 날짜 필터: 과거 대회 제외
    const eventDate = new Date(m.date);
    eventDate.setHours(0, 0, 0, 0);
    if (eventDate < today) return false;

    // 2. 기존 필터들...
    // ...
  }).sort((a, b) => new Date(a.date).getTime() - new Date(b.date).getTime());
}, [filters, marathonData]);
```

#### 5.2.2 URL 정규화 (Feature 3)

```typescript
// hooks/useMarathons.ts - transformEvent
function transformEvent(apiEvent: MarathonAPIEvent): MarathonEvent {
  // URL 정규화
  let normalizedUrl = apiEvent.registrationUrl || '';
  if (normalizedUrl && !normalizedUrl.startsWith('http')) {
    normalizedUrl = `https://${normalizedUrl}`;
  }

  return {
    id: apiEvent.id,
    name: apiEvent.name,
    date: apiEvent.date,
    region: apiEvent.country === 'INTL' ? '해외' : (apiEvent.region || '기타'),
    locationDetail: apiEvent.locationDetail || '',
    distances: apiEvent.distances,
    registrationUrl: normalizedUrl,
    tags: apiEvent.tags,
    isPopular: apiEvent.isPopular,
    notes: apiEvent.organizer ? `주최: ${apiEvent.organizer}` : undefined,
  };
}
```

#### 5.2.3 국가 필터 (Feature 2)

```typescript
// types.ts
export interface FilterState {
  months: number[];
  regions: string[];
  countries: string[]; // 추가
  distances: string[];
  searchQuery: string;
}

// App.tsx
const [filters, setFilters] = useState<FilterState>({
  months: [],
  regions: [],
  countries: [], // 추가
  distances: [],
  searchQuery: '',
});

// 필터링 로직
const matchCountry = filters.countries.length === 0 ||
  filters.countries.some(fc => {
    if (fc === '국내') return m.region !== '해외';
    if (fc === '해외') return m.region === '해외';
    return true;
  });
```

### 5.3 데이터 변환 전략

#### Before (현재)
```json
{
  "region": "기타",
  "distances": ["풀", "하프"],
  "registrationUrl": "www.example.com"
}
```

#### After (개선)
```json
{
  "region": "해외",
  "distances": ["Full", "Half"],
  "registrationUrl": "https://www.example.com"
}
```

---

## 6. Implementation Plan (구현 계획)

### 6.1 Phase 1: 데이터 레이어 수정 (30분)

**작업 순서:**
1. `hooks/useMarathons.ts` 수정
   - `transformEvent()`: URL 정규화 로직 추가
   - `transformEvent()`: 지역 "해외" 매핑 추가

2. `constants.ts` 수정
   - `REGIONS` 배열에 `'해외'` 추가
   - `DISTANCES` 배열: `['Full', 'Half', '10km', '5km', '울트라', '기타']`
   - `COUNTRIES` 상수 추가: `['국내', '해외']`

**검증:**
```bash
# 브라우저 콘솔에서
marathonData[0].registrationUrl // "https://..."로 시작해야 함
marathonData.filter(m => m.region === '해외').length // 국외 대회 수
```

### 6.2 Phase 2: 필터 레이어 수정 (30분)

**작업 순서:**
1. `types.ts` 수정
   - `FilterState`에 `countries: string[]` 추가

2. `App.tsx` 수정
   - `filters` 초기값에 `countries: []` 추가
   - `filteredMarathons` 로직:
     - 날짜 필터 추가 (과거 제외)
     - 국가 필터 추가
     - 거리 필터 Full/Half 대응
   - `handleResetFilters`에 `countries: []` 추가

**검증:**
```javascript
// 필터 테스트
filters.countries = ['국내']
// → 국내 대회만 표시

filters.countries = ['해외']
// → 해외 대회만 표시
```

### 6.3 Phase 3: UI 레이어 수정 (30분)

**작업 순서:**
1. `components/FilterBar.tsx` 수정
   - 국가 필터 드롭다운 추가
   - 선택된 국가 배지 표시
   - 드롭다운 상태 관리 (`openDropdown` 타입 확장)

**UI 레이아웃:**
```
┌─────────────────────────────────────────┐
│  [MONTH ▼]  [REGION ▼]  [COUNTRY ▼]    │
│                          ^새로 추가      │
│  [3M] [서울] [국내]  ← 선택 배지        │
└─────────────────────────────────────────┘
```

**검증:**
- 국가 드롭다운 클릭 → "국내", "해외" 선택 가능
- 선택 시 배지 표시
- 필터 동작 확인

### 6.4 Phase 4: 통합 테스트 (30분)

**테스트 시나리오:**

| 테스트 케이스 | 입력 | 예상 결과 |
|-------------|------|----------|
| TC1: 과거 대회 필터링 | 페이지 로드 | 오늘 이후 대회만 표시 |
| TC2: 국내 필터 | countries = ['국내'] | 국내 대회만 표시 |
| TC3: 해외 필터 | countries = ['해외'] | 해외 대회만 표시 |
| TC4: URL 클릭 | 등록 버튼 클릭 | 새 탭에서 https://... 열림 |
| TC5: 거리 표시 | Full 마라톤 카드 | "Full" 배지 표시 |
| TC6: 지역 표시 | 해외 대회 카드 | "해외 · Qatar" 표시 |
| TC7: 복합 필터 | 국내 + 3월 + Full | 조건 만족 대회만 표시 |

---

## 7. Dependencies (의존성)

### 7.1 기술 의존성

| 항목 | 현재 버전 | 필요 버전 | 비고 |
|------|----------|----------|------|
| React | 19.2.3 | 19.2.3 | 변경 없음 |
| TypeScript | 5.8.2 | 5.8.2 | 타입 추가 |
| Lucide React | 0.562.0 | 0.562.0 | 변경 없음 |

### 7.2 데이터 의존성

- ✅ `/public/data/marathons.json`: 이미 존재
- ✅ `country` 필드: 이미 존재 (`KR`, `INTL`)
- ✅ `distances` 필드: 이미 존재 (표기만 변경)

### 7.3 스크래퍼 의존성 (선택적)

**⚠️ 참고**: 거리 표기 통일을 위해 스크래퍼 수정 권장 (별도 작업)
```typescript
// scripts/scrapers/processors/normalizer.ts
const normalizeDistance = (raw: string): string => {
  if (/풀|full|42\.195|42km/i.test(raw)) return 'Full';
  if (/하프|half|21\.0975|21km/i.test(raw)) return 'Half';
  // ...
};
```

---

## 8. Risk Assessment (리스크 평가)

### 8.1 기술 리스크

| 리스크 | 확률 | 영향도 | 완화 방안 |
|-------|------|--------|----------|
| 과거 대회 필터링으로 즐겨찾기 데이터 손실 | 중 | 중 | 즐겨찾기는 필터 제외 처리 |
| URL 정규화로 인한 잘못된 링크 생성 | 저 | 중 | http/https 모두 지원 |
| 거리 표기 변경으로 기존 필터 작동 안 함 | 중 | 중 | Full/Half 매핑 추가 |
| 국가 필터로 인한 성능 저하 | 저 | 저 | useMemo 활용 (이미 적용) |

### 8.2 데이터 리스크

| 리스크 | 확률 | 영향도 | 완화 방안 |
|-------|------|--------|----------|
| 기존 JSON 데이터에 country 없음 | 저 | 중 | transformEvent에서 기본값 처리 |
| URL 형식 예외 케이스 존재 | 중 | 중 | 정규 표현식으로 다양한 형식 지원 |
| 거리 표기 일관성 없음 | 고 | 저 | 스크래퍼 수정 (별도 작업) |

### 8.3 UX 리스크

| 리스크 | 확률 | 영향도 | 완화 방안 |
|-------|------|--------|----------|
| 사용자가 과거 대회를 찾을 수 없음 | 중 | 중 | Phase 2에서 아카이브 페이지 추가 |
| 필터 버튼이 너무 많아져서 복잡함 | 저 | 저 | 3개 드롭다운 유지 (적절) |

---

## 9. Timeline (일정)

### 9.1 예상 소요 시간

| Phase | 작업 | 예상 시간 | 담당 |
|-------|------|----------|------|
| Phase 1 | 데이터 레이어 수정 | 30분 | Dev |
| Phase 2 | 필터 레이어 수정 | 30분 | Dev |
| Phase 3 | UI 레이어 수정 | 30분 | Dev |
| Phase 4 | 통합 테스트 | 30분 | QA + Dev |
| **Total** | | **2시간** | |

### 9.2 마일스톤

```
Day 1 (2시간)
├─ 09:00-09:30  Phase 1: 데이터 레이어
├─ 09:30-10:00  Phase 2: 필터 레이어
├─ 10:00-10:30  Phase 3: UI 레이어
└─ 10:30-11:00  Phase 4: 테스트 + 배포

✅ 완료 기준: 5개 테스트 케이스 모두 통과
```

---

## 10. Success Criteria (성공 기준)

### 10.1 기능 완성도

- [ ] **Feature 1**: 과거 대회가 목록에서 자동 제외됨
- [ ] **Feature 2**: 국내/해외 필터 드롭다운 작동
- [ ] **Feature 3**: 모든 등록 URL이 https://로 시작
- [ ] **Feature 4**: 거리 배지에 "Full", "Half" 표시
- [ ] **Feature 5**: 국외 대회 지역이 "해외"로 표시

### 10.2 품질 기준

- [ ] TypeScript 타입 에러 0건
- [ ] ESLint 경고 0건
- [ ] 브라우저 콘솔 에러 0건
- [ ] 모든 필터 조합 정상 작동
- [ ] 모바일 반응형 레이아웃 유지

### 10.3 사용자 경험 기준

- [ ] 첫 화면에 "과거 대회" 표시 안 됨
- [ ] 국내 필터 클릭 시 즉시 적용 (지연 없음)
- [ ] 등록 버튼 클릭 시 새 탭에서 외부 사이트 열림
- [ ] 거리 표기 일관성 (영문 표기)
- [ ] 해외 대회 위치 명확 표시

---

## 11. Rollback Plan (롤백 계획)

### 11.1 롤백 트리거

다음 상황 발생 시 즉시 롤백:
1. URL 클릭 시 404 에러율 > 10%
2. 필터 작동 불가
3. 과거 대회가 즐겨찾기에서 삭제됨
4. TypeScript 빌드 실패

### 11.2 롤백 절차

```bash
# 1. Git 이전 커밋으로 복구
git log --oneline | head -5
git revert <commit-hash>

# 2. 의존성 재설치 (필요 시)
npm install

# 3. 개발 서버 재시작
npm run dev

# 4. 배포
npm run build
```

---

## 12. Post-Launch Monitoring (출시 후 모니터링)

### 12.1 모니터링 지표

| 지표 | 측정 도구 | 임계값 |
|------|----------|--------|
| 페이지 로딩 시간 | Browser DevTools | < 2초 |
| 필터 응답 시간 | React DevTools Profiler | < 100ms |
| 등록 버튼 클릭 성공률 | Google Analytics | > 95% |
| 사용자 피드백 | 고객 지원 | 긍정 > 80% |

### 12.2 A/B 테스트 (선택적)

**테스트 항목**: 과거 대회 완전 제거 vs 하단 표시
- **그룹 A (50%)**: 과거 대회 완전 숨김
- **그룹 B (50%)**: 과거 대회 하단에 "지난 대회" 섹션 표시
- **측정**: 사용자 만족도, 체류 시간

---

## 13. Future Enhancements (향후 개선)

### Phase 2 (2주 후)

- [ ] 과거 대회 아카이브 페이지
- [ ] 대회 캘린더 뷰 (월간 달력)
- [ ] 국가별 상세 필터 (일본, 미국, 유럽 등)

### Phase 3 (1개월 후)

- [ ] 거리 필터 드록다운 UI 완성
- [ ] 대회 상세 모달
- [ ] 공유 기능 (SNS 링크 복사)

---

## 14. Approval & Sign-off

### 14.1 체크리스트

- [ ] 문제 정의 명확함
- [ ] 기술 접근 방식 검토 완료
- [ ] 리스크 평가 완료
- [ ] 구현 일정 합의
- [ ] 성공 기준 정의

### 14.2 승인

| 역할 | 이름 | 승인 날짜 | 서명 |
|------|------|----------|------|
| Product Owner | - | - | - |
| Tech Lead | Claude | 2026-01-21 | ✅ |
| QA Lead | - | - | - |

---

## 15. References (참고 자료)

### 15.1 관련 문서

- [Data Automation Plan](./01_data-automation.plan.md)
- [Gorunning Date Parsing Fix](./02_gorunning-date-parsing-fix.plan.md)
- [UI/UX Analysis Report](../../claudedocs/bkit-case-study-marathon-scraper.md)

### 15.2 외부 참고

- [React useMemo 최적화](https://react.dev/reference/react/useMemo)
- [URL 정규화 베스트 프랙티스](https://developer.mozilla.org/en-US/docs/Web/API/URL)

---

## 16. Change Log (변경 이력)

| 날짜 | 버전 | 변경 내용 | 작성자 |
|------|------|----------|--------|
| 2026-01-21 | 1.0 | 초안 작성 | Claude |

---

## 📝 Next Steps

1. ✅ Plan 문서 검토 및 승인
2. ⏭️ Design 문서 작성: `/pdca-design ui-ux-critical-improvements`
3. ⏭️ 구현 시작: Phase 1부터 단계적 진행
4. ⏭️ 테스트 및 배포

---

**Document Status**: ✅ Ready for Review
**Estimated Effort**: 2 hours
**Priority**: 🔴 P0 (Critical)
